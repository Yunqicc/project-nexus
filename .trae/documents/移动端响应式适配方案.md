## 目标
- 全面优化 Project Nexus 在移动端（手机/平板）的显示效果
- 重点修复导航栏折叠、间距过大、布局错位等问题

## 实施方案
1. **导航栏 (Header)**：
   - 增加移动端汉堡菜单（Hamburger Menu）
   - 在小屏幕下隐藏水平导航链接，改为点击展开的下拉菜单
2. **Hero 区域**：
   - 调整标题字体大小 (`text-3xl` -> `text-2xl`)
   - 优化按钮组布局（`flex-col` 或 `flex-wrap`），防止挤压
3. **留言墙 (VibeWall)**：
   - 将表单布局从 Grid 改为 Flex Column（小屏幕）
   - 调整输入框高度与间距，适配手指触控
4. **项目橱窗 (ProjectGallery)**：
   - 保持 Grid 布局，但调整列数（`grid-cols-1` -> `grid-cols-2` -> `grid-cols-3`）
   - 优化卡片内边距与图片高度

## 执行步骤
1. 修改 `App.tsx` 实现响应式 Header
2. 优化 `Hero.tsx` 的按钮组布局
3. 调整 `VibeWall.tsx` 的表单响应式类名
4. 提交代码并推送

确认后，我将从 Header 开始逐个优化。